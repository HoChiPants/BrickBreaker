var isAlphaOrdered = function (a, b) {
	return a.toLowerCase() > b.toLowerCase();
};

module.exports = function (arr) {
	for (var i = 0, l = arr.length; i < l; i++) {
		if (i === 0) continue;
		if (!isAlphaOrdered(arr[i], arr[i - 1])) {
			return false;
		}
	}

	return true;
};
